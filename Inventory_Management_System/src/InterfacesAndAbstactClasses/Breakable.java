package InterfacesAndAbstactClasses;

// Interface defining methods for breakable items
public interface Breakable {
   // Method to check if the item is breakable
   boolean isBreakable();

    // Method to handle item breakage
    void handleBreakage();
}
